{
  "name": "aacad325-workroom",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/editor.js --bundle --format=esm --minify --outfile=dist/editor.js",
    "dev": "npm run build && python3 -m http.server 5173 --directory dist"
  },
  "dependencies": {
    "@tiptap/core": "^3.31.3",
    "@tiptap/extension-placeholder": "^3.31.3",
    "@tiptap/pm": "^3.31.3",
    "@tiptap/starter-kit": "^3.31.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
